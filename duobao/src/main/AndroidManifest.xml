<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.peng.duobao">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_one"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoadingPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
       <!-- <receiver android:name=".MainActivity$broadreciver">
            <intent-filter android:priority="100">
                <action android:name="Four_Fragment"></action>
            </intent-filter>
        </receiver>-->
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Activity_FirstFragment.TestActivity"></activity>
        <activity android:name=".WebView.WebActivity"></activity>
        <activity android:name=".WebView.Register_Web"></activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
</manifest>